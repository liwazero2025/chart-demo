---
description: 以专家视角审阅与编写代码；含 Git 变更审阅提示（SOLID、安全、竞态、错误、性能、边界）
alwaysApply: false
---

# 代码专家视角（审阅与实现）

在修改、审阅或建议代码时，主动从以下维度检查；发现风险要指出并给出可落地的改法，避免空泛批评。

## 审阅当前 Git 变更（提示语）

针对**工作区 / 暂存区 / 与基线差异**做审阅时，以如下清单为纲（可先读 `git diff` 再下结论）：

> Review current git changes for SOLID violations, security risks, race conditions, error handling issues, performance problems, and boundary condition bugs.

## SOLID 与可维护性

- **S**：职责是否单一；是否该拆模块/函数/类型。
- **O**：扩展是否不必改大量现有代码（策略、配置、插件化等）。
- **L**：子类型/实现是否可替换父类型约定而不破坏调用方。
- **I**：接口是否臃肿；客户端是否被迫依赖不需要的方法。
- **D**：是否依赖抽象而非具体实现；依赖方向是否倒置合理。

同时关注：命名与边界清晰、重复与耦合、测试与类型是否支撑重构。

## Security（安全）

- 输入校验、边界与权限：用户/外部数据不可信；最小权限与敏感信息不落日志/前端。
- 常见漏洞意识：XSS、注入、不安全反序列化、开放重定向、硬编码密钥、依赖已知 CVE。
- 浏览器/前端：避免 `dangerouslySetInnerHTML` 等未消毒用法；敏感 token 的存储与传输方式。

## Performance（性能）

- 算法与数据结构是否匹配数据规模；是否存在明显 O(n²) 或可避免的重复计算/请求。
- 渲染与订阅：不必要的重渲染、大列表无虚拟化、effect 依赖与清理是否正确。
- 网络与资源：缓存、去抖节流、bundle 体积与懒加载是否值得做（有依据再建议）。

## Error handling（错误处理）

- 失败路径是否明确：可恢复 vs 不可恢复；用户可见反馈与可观测性（日志/指标）。
- 异步与边界：`async`/`Promise`、fetch、解析失败是否有处理；不要吞掉错误。
- 类型与契约：错误类型是否可区分；避免 `catch (e) {}` 或仅 `console.log` 无后续。

## Concurrency（竞态与顺序）

- 异步完成顺序是否与 UI/状态假设一致；是否在卸载或依赖变化后仍 `setState`。
- 重复触发（连点、Strict Mode、重试）是否会导致重复提交或覆盖更新；是否需要取消、锁、或 `AbortController`。

## Boundary conditions（边界条件）

- 空数组/空对象、`null`/`undefined`、0、负数、极大值、非法格式等是否显式处理。
- 时间与排序边界：空区间、单条、首尾、跨时区或时钟回拨等是否安全。

## 输出习惯

- 先结论（是否通过 / 主要风险），再分点说明；严重问题标优先级。
- 建议附带简短示例或伪代码时，须与项目现有风格一致。
